# Кропачев Александр БАС-1-2024 Лабораторная №7

# Задание 1

## Задача 1

### Текст задачи

В файле найти сумму максимального и минимального элементов.

### Алгоритм решения

Текстовый файл заполняется случайными целыми числами, каждое число записывается в отдельной строке.
После этого файл считывается построчно с помощью StreamReader. Каждая строка преобразуется в число. В процессе чтения определяется минимальный и максимальный элементы.
В конце вычисляется их сумма и выводится результат.

### Тестирование

Скриншоты результата работы программы 


# Задание 2

## Задача 1

### Текст задачи

Вычислить сумму чётных элементов.

### Алгоритм решения

Файл содержит несколько чисел в одной строке, разделённых пробелами.
Сначала файл заполняется случайными числами. Затем при чтении каждая строка разбивается на элементы с помощью метода Split.
Каждый элемент преобразуется в число, и если число является чётным, оно добавляется к общей сумме.
В результате выводится сумма всех чётных чисел.

### Тестирование

Скриншоты результата работы программы


# Задание 3

## Задача 1

### Текст задачи

Создать новый текстовый файл, каждая строка которого содержит первый символ
соответствующей строки исходного файла.

### Алгоритм решения

Исходный файл содержит текст.
Программа считывает файл построчно и для каждой строки берёт первый символ. Перед этим выполняется проверка, чтобы строка не была пустой.
Полученные символы записываются в новый текстовый файл с помощью StreamWriter.

### Тестирование

Скриншоты результата работы программы


# Задание 4

## Задача 1

### Текст задачи

Получить в другом файле последовательного доступа все компоненты исходного файла, кроме
тех, которые кратны k.

### Алгоритм решения

В задании используется бинарный файл, содержащий целые числа.
Файл заполняется случайными значениями с помощью BinaryWriter. Затем он считывается с использованием BinaryReader.
Чтение продолжается до тех пор, пока позиция в потоке меньше длины файла. Каждое число проверяется: если оно не кратно заданному числу k, оно записывается в новый бинарный файл.


### Тестирование

Скриншоты результата работы программы


# Задание 5

## Задача 1

### Текст задачи

Файл содержит сведения об игрушках: название игрушки, ее стоимость в рублях и возрастные
границы (например, игрушка может предназначаться для детей от двух до пяти лет). Получить
сведения о том, можно ли подобрать игрушку, любую, кроме мяча, подходящую ребенку трех лет

### Алгоритм решения

Создаётся класс Toy, описывающий игрушку.
Список объектов этого класса сериализуется в XML-файл с помощью XmlSerializer.
Затем файл десериализуется обратно в список объектов. Программа проверяет, существует ли игрушка (кроме мяча), подходящая ребёнку 3 лет по возрастному диапазону.
Если такая игрушка есть — выводится положительный результат.

### Тестирование

Скриншоты результата работы программы


# Задание 6

## Задача 1

### Текст задачи

Составить программу, которая переворачивает список L, т.е. изменяет ссылки в этом списке так,
чтобы его элементы оказались расположенными в обратном порядке.

### Алгоритм решения

Создаётся список.
Переворот списка выполняется с помощью двух указателей: один указывает на начало списка, второй — на конец.
Элементы меняются местами, после чего указатели сдвигаются к центру. Процесс продолжается, пока указатели не встретятся.
В результате список переворачивается без создания нового списка.

### Тестирование

Скриншоты результата работы программы


# Задание 7

## Задача 1

### Текст задачи

В списке L справа и слева от элемента E вставить элемент F.

### Алгоритм решения

Используется структура LinkedList.
Сначала создаётся список и заполняется элементами. Затем с помощью метода Find находится узел с заданным значением.
После этого с помощью методов AddBefore и AddAfter выполняется вставка нового элемента слева и справа от найденного узла.
Таким образом элементы добавляются вокруг заданного элемента.

### Тестирование

Скриншоты результата работы программы


# Задание 8

## Задача 1

### Текст задачи

Есть перечень дискотек города. Студенты группы любят посещать дискотеки. Известно для
каждого студента, в каких дискотеках он побывал. Определить:
• в какие дискотеки из перечня ходили все студенты группы;
• в какие дискотеки из перечня ходили некоторые студенты группы;
• в какие дискотеки из перечня не ходил ни один из студентов группы?

### Алгоритм решения

Используются множества HashSet для хранения дискотек.
Для каждого студента создаётся своё множество посещённых дискотек.
С помощью IntersectWith определяется пересечение множеств — дискотеки, которые посещали все студенты.
С помощью UnionWith определяется объединение — дискотеки, которые посещал хотя бы один студент.
С помощью ExceptWith определяется разность — дискотеки, которые не посещал ни один студент.
Результаты выводятся на экран.

### Тестирование

Скриншоты результата работы программы


# Задание 9

## Задача 1

### Текст задачи

Файл содержит текст на русском языке. Напечатать в алфавитном порядке символы, которые
встречаются хотя бы однажды в словах с чётными номерами (нумерацию вести с 1).

### Алгоритм решения

Текст считывается из файла и разбивается на слова с помощью Split.
Выбираются слова с чётными номерами (учитывая, что нумерация начинается с 1).
Все символы этих слов добавляются в HashSet, что позволяет исключить повторения.
Затем множество преобразуется в список, сортируется методом Sort и выводится в алфавитном порядке.

### Тестирование

Скриншоты результата работы программы


# Задание 10

## Задача 1

### Текст задачи

В некотором вузе абитуриенты проходили предварительное тестирование, по результатам
которого они могут быть допущены к сдаче вступительных экзаменов в первом потоке.
Тестирование проводится по трём предметам, по каждому предмету абитуриент может набрать
от 0 100 баллов. При этом к сдаче экзаменов в первом потоке допускаются абитуриенты,
набравшие по результатам тестирования не менее 30 баллов по каждому из трёх предметов,
причём сумма баллов должна быть не менее 140. На вход программы подаются сведения о
результатах предварительного тестирования. Известно, что общее количество участников
тестирования не превосходит 500.
В первой строке вводится количество абитуриентов, принимавших участие в тестировании, N.
Далее следуют N строк, имеющих следующий формат:
<Фамилия><Имя><Баллы>
Здесь <Фамилия> – строка, состоящая не более чем из 20 символов; <Имя> – строка, состоящая не
более чем из 15 символов, <Баллы> – строка, содержащая два целых числа, разделенных
пробелом – баллы, полученные на тестировании по каждому из трёх предметов. При этом
<Фамилия> и <Имя>, <Имя> и <Баллы> разделены одним пробелом. Пример входной строки:
Романов Вельямин 48 39 55
Напишите программу, которая будет выводить на экран фамилии и имена абитуриентов,
допущенных к сдаче экзаменов в первом потоке. При этом фамилии должны выводиться в
алфавитном порядке.

### Алгоритм решения

Из файла считывается количество абитуриентов, затем их данные.
Каждая строка разбивается на фамилию, имя и три оценки.
Проверяется условие допуска:
не менее 30 баллов по каждому предмету
сумма баллов не менее 140
Подходящие абитуриенты добавляются в SortedList, который автоматически сортирует их по ключу (фамилии и имени).

После этого список выводится в алфавитном порядке.

### Тестирование

Скриншоты результата работы программы






